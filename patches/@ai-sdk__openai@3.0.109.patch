diff --git a/dist/index.js b/dist/index.js
index 48ea06e..f1f05cf 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -60,16 +60,17 @@ function getOpenAILanguageModelCapabilities(modelId) {
   const isGptChatModel = (gptVersion == null ? void 0 : gptVersion.minor) == null && ((_b = (_a2 = gptVersion == null ? void 0 : gptVersion.variant) == null ? void 0 : _a2.startsWith("chat")) != null ? _b : false);
   const isGptNanoModel = (_d = (_c = gptVersion == null ? void 0 : gptVersion.variant) == null ? void 0 : _c.startsWith("nano")) != null ? _d : false;
   const isGpt6OrLaterModel = gptVersion != null && gptVersion.major >= 6;
+  const isGpt6NonReasoningModel = /^gpt-6-(?:sol|luna)(?:-|$)/.test(modelId);
   const supportsFlexProcessing = oSeriesVersion != null && oSeriesVersion >= 3 || gptVersion != null && gptVersion.major >= 5 && !isGptChatModel;
   const supportsPriorityProcessing = modelId.startsWith("gpt-4") || gptVersion != null && gptVersion.major >= 5 && !isGptNanoModel && !isGptChatModel || oSeriesVersion != null && oSeriesVersion >= 3;
   const isReasoningModel = oSeriesVersion != null || gptVersion != null && gptVersion.major >= 5 && !isGptChatModel;
-  const supportsNonReasoningParameters = !isGpt6OrLaterModel && gptVersion != null && (gptVersion.major > 5 || gptVersion.major === 5 && ((_e = gptVersion.minor) != null ? _e : 0) >= 1);
+  const supportsNonReasoningParameters = isGpt6NonReasoningModel || (!isGpt6OrLaterModel && gptVersion != null && (gptVersion.major > 5 || gptVersion.major === 5 && ((_e = gptVersion.minor) != null ? _e : 0) >= 1));
   const systemMessageMode = isReasoningModel ? "developer" : "system";
   return {
     supportsFlexProcessing,
     supportsPriorityProcessing,
     supportsConfigurationUpdate: isGpt6OrLaterModel,
-    supportedReasoningEfforts: isGpt6OrLaterModel ? ["low", "medium", "high", "xhigh", "max"] : void 0,
+    supportedReasoningEfforts: isGpt6OrLaterModel ? isGpt6NonReasoningModel ? ["none", "low", "medium", "high", "xhigh", "max"] : ["low", "medium", "high", "xhigh", "max"] : void 0,
     isReasoningModel,
     systemMessageMode,
     supportsNonReasoningParameters
@@ -1012,7 +1013,7 @@ var OpenAIChatLanguageModel = class {
         details: "promptCacheRetention is not supported by GPT-6 and later models; use promptCacheOptions instead"
       });
     }
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (openaiOptions.reasoningEffort !== "none" || !modelCapabilities.supportsNonReasoningParameters) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -2025,7 +2026,8 @@ var openaiImageResponseSchema = (0, import_provider_utils12.lazySchema)(
       created: import_v48.z.number().nullish(),
       data: import_v48.z.array(
         import_v48.z.object({
-          b64_json: import_v48.z.string(),
+          b64_json: import_v48.z.string().nullish(),
+          url: import_v48.z.string().nullish(),
           revised_prompt: import_v48.z.string().nullish()
         })
       ),
@@ -2213,7 +2215,11 @@ var OpenAIImageModel = class {
         fetch: this.config.fetch
       });
       return {
-        images: response2.data.map((item) => item.b64_json),
+        images: response2.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
         warnings,
         usage: response2.usage != null ? {
           inputTokens: (_e = response2.usage.input_tokens) != null ? _e : void 0,
@@ -2280,7 +2286,11 @@ var OpenAIImageModel = class {
       fetch: this.config.fetch
     });
     return {
-      images: response.data.map((item) => item.b64_json),
+      images: response.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
       warnings,
       usage: response.usage != null ? {
         inputTokens: (_i = response.usage.input_tokens) != null ? _i : void 0,
@@ -3280,6 +3290,9 @@ function isFileId(data, prefixes) {
   if (!prefixes) return false;
   return prefixes.some((prefix) => data.startsWith(prefix));
 }
+function toResponsesItemId(id, prefix) {
+  return typeof id === "string" && id.startsWith(prefix) ? id : void 0;
+}
 async function convertToOpenAIResponsesInput({
   prompt,
   toolNameMapping,
@@ -3433,7 +3446,7 @@ async function convertToOpenAIResponsesInput({
           switch (part.type) {
             case "text": {
               const providerOpts = (_a2 = part.providerOptions) == null ? void 0 : _a2[providerOptionsName];
-              const id = providerOpts == null ? void 0 : providerOpts.itemId;
+              const id = toResponsesItemId(providerOpts == null ? void 0 : providerOpts.itemId, "msg");
               const phase = providerOpts == null ? void 0 : providerOpts.phase;
               if (hasConversation && id != null) {
                 break;
@@ -3443,9 +3456,11 @@ async function convertToOpenAIResponsesInput({
                 break;
               }
               input.push({
+                type: "message",
                 role: "assistant",
                 content: [{ type: "output_text", text: part.text }],
                 id,
+                status: "completed",
                 ...phase != null && { phase }
               });
               break;
@@ -3666,7 +3681,7 @@ async function convertToOpenAIResponsesInput({
                 providerOptions: part.providerOptions,
                 schema: openaiResponsesReasoningProviderOptionsSchema
               });
-              const reasoningId = providerOptions2 == null ? void 0 : providerOptions2.itemId;
+              const reasoningId = toResponsesItemId(providerOptions2 == null ? void 0 : providerOptions2.itemId, "rs");
               if ((hasConversation || hasPreviousResponseId) && reasoningId != null) {
                 break;
               }
@@ -3681,6 +3696,12 @@ async function convertToOpenAIResponsesInput({
                       summary: []
                     };
                   }
+                } else if ((providerOptions2 == null ? void 0 : providerOptions2.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   const summaryParts = [];
                   if (part.text.length > 0) {
@@ -3724,6 +3745,12 @@ async function convertToOpenAIResponsesInput({
                     encrypted_content: encryptedContent,
                     summary: summaryParts
                   });
+                } else if ((providerOptions2 == null ? void 0 : providerOptions2.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   warnings.push({
                     type: "other",
@@ -4006,21 +4033,22 @@ ${text}`,
     }
   }
   if (!store && input.some(
-    (item) => "type" in item && item.type === "reasoning" && item.encrypted_content == null
+    (item) => "type" in item && item.type === "reasoning" && item.encrypted_content == null && !("content" in item && item.content != null)
   )) {
     warnings.push({
       type: "other",
       message: "Reasoning parts without encrypted content are not supported when store is false. Skipping reasoning parts."
     });
     input = input.filter(
-      (item) => !("type" in item) || item.type !== "reasoning" || item.encrypted_content != null
+      (item) => !("type" in item) || item.type !== "reasoning" || item.encrypted_content != null || "content" in item && item.content != null
     );
   }
   return { input, warnings };
 }
 var openaiResponsesReasoningProviderOptionsSchema = import_v421.z.object({
   itemId: import_v421.z.string().nullish(),
-  reasoningEncryptedContent: import_v421.z.string().nullish()
+  reasoningEncryptedContent: import_v421.z.string().nullish(),
+  rawReasoningContent: import_v421.z.boolean().nullish()
 });
 
 // src/responses/map-openai-responses-finish-reason.ts
@@ -4658,6 +4686,11 @@ var openaiResponsesChunkSchema = (0, import_provider_utils28.lazySchema)(
         summary_index: import_v422.z.number(),
         delta: import_v422.z.string()
       }),
+      import_v422.z.object({
+        type: import_v422.z.literal("response.reasoning_text.delta"),
+        item_id: import_v422.z.string(),
+        delta: import_v422.z.string()
+      }),
       import_v422.z.object({
         type: import_v422.z.literal("response.reasoning_summary_part.done"),
         item_id: import_v422.z.string(),
@@ -4752,7 +4785,7 @@ var openaiResponsesResponseSchema = (0, import_provider_utils28.lazySchema)(
                       index: import_v422.z.number()
                     })
                   ])
-                )
+                ).nullish().transform((v) => v ?? [])
               })
             )
           }),
@@ -5960,7 +5993,7 @@ var OpenAIResponsesLanguageModel = class {
         details: "promptCacheRetention is not supported by GPT-6 and later models; use promptCacheOptions instead"
       });
     }
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (!((openaiOptions == null ? void 0 : openaiOptions.reasoningEffort) === "none" && modelCapabilities.supportsNonReasoningParameters)) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -5979,7 +6012,7 @@ var OpenAIResponsesLanguageModel = class {
           });
         }
       }
-      if (modelCapabilities.supportedReasoningEfforts != null && (baseArgs.top_logprobs != null || ((_i = baseArgs.include) == null ? void 0 : _i.includes("message.output_text.logprobs")))) {
+      if (modelCapabilities.supportedReasoningEfforts != null && !(resolvedReasoningEffort === "none" && modelCapabilities.supportsNonReasoningParameters) && (baseArgs.top_logprobs != null || ((_i = baseArgs.include) == null ? void 0 : _i.includes("message.output_text.logprobs")))) {
         baseArgs.top_logprobs = void 0;
         const filteredInclude = (_j = baseArgs.include) == null ? void 0 : _j.filter(
           (value) => value !== "message.output_text.logprobs"
@@ -7411,6 +7444,17 @@ var OpenAIResponsesLanguageModel = class {
                   }
                 }
               });
+            } else if (value.type === "response.reasoning_text.delta") {
+              controller.enqueue({
+                type: "reasoning-delta",
+                id: `${value.item_id}:0`,
+                delta: value.delta,
+                providerMetadata: {
+                  [providerOptionsName]: {
+                    itemId: value.item_id
+                  }
+                }
+              });
             } else if (value.type === "response.reasoning_summary_part.done") {
               if (store) {
                 controller.enqueue({
diff --git a/dist/index.mjs b/dist/index.mjs
index 2648382..df1414e 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -46,16 +46,17 @@ function getOpenAILanguageModelCapabilities(modelId) {
   const isGptChatModel = (gptVersion == null ? void 0 : gptVersion.minor) == null && ((_b = (_a2 = gptVersion == null ? void 0 : gptVersion.variant) == null ? void 0 : _a2.startsWith("chat")) != null ? _b : false);
   const isGptNanoModel = (_d = (_c = gptVersion == null ? void 0 : gptVersion.variant) == null ? void 0 : _c.startsWith("nano")) != null ? _d : false;
   const isGpt6OrLaterModel = gptVersion != null && gptVersion.major >= 6;
+  const isGpt6NonReasoningModel = /^gpt-6-(?:sol|luna)(?:-|$)/.test(modelId);
   const supportsFlexProcessing = oSeriesVersion != null && oSeriesVersion >= 3 || gptVersion != null && gptVersion.major >= 5 && !isGptChatModel;
   const supportsPriorityProcessing = modelId.startsWith("gpt-4") || gptVersion != null && gptVersion.major >= 5 && !isGptNanoModel && !isGptChatModel || oSeriesVersion != null && oSeriesVersion >= 3;
   const isReasoningModel = oSeriesVersion != null || gptVersion != null && gptVersion.major >= 5 && !isGptChatModel;
-  const supportsNonReasoningParameters = !isGpt6OrLaterModel && gptVersion != null && (gptVersion.major > 5 || gptVersion.major === 5 && ((_e = gptVersion.minor) != null ? _e : 0) >= 1);
+  const supportsNonReasoningParameters = isGpt6NonReasoningModel || (!isGpt6OrLaterModel && gptVersion != null && (gptVersion.major > 5 || gptVersion.major === 5 && ((_e = gptVersion.minor) != null ? _e : 0) >= 1));
   const systemMessageMode = isReasoningModel ? "developer" : "system";
   return {
     supportsFlexProcessing,
     supportsPriorityProcessing,
     supportsConfigurationUpdate: isGpt6OrLaterModel,
-    supportedReasoningEfforts: isGpt6OrLaterModel ? ["low", "medium", "high", "xhigh", "max"] : void 0,
+    supportedReasoningEfforts: isGpt6OrLaterModel ? isGpt6NonReasoningModel ? ["none", "low", "medium", "high", "xhigh", "max"] : ["low", "medium", "high", "xhigh", "max"] : void 0,
     isReasoningModel,
     systemMessageMode,
     supportsNonReasoningParameters
@@ -1008,7 +1009,7 @@ var OpenAIChatLanguageModel = class {
         details: "promptCacheRetention is not supported by GPT-6 and later models; use promptCacheOptions instead"
       });
     }
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (openaiOptions.reasoningEffort !== "none" || !modelCapabilities.supportsNonReasoningParameters) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -2056,7 +2057,8 @@ var openaiImageResponseSchema = lazySchema7(
       created: z8.number().nullish(),
       data: z8.array(
         z8.object({
-          b64_json: z8.string(),
+          b64_json: z8.string().nullish(),
+          url: z8.string().nullish(),
           revised_prompt: z8.string().nullish()
         })
       ),
@@ -2247,7 +2249,11 @@ var OpenAIImageModel = class {
         fetch: this.config.fetch
       });
       return {
-        images: response2.data.map((item) => item.b64_json),
+        images: response2.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
         warnings,
         usage: response2.usage != null ? {
           inputTokens: (_e = response2.usage.input_tokens) != null ? _e : void 0,
@@ -2314,7 +2320,11 @@ var OpenAIImageModel = class {
       fetch: this.config.fetch
     });
     return {
-      images: response.data.map((item) => item.b64_json),
+      images: response.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
       warnings,
       usage: response.usage != null ? {
         inputTokens: (_i = response.usage.input_tokens) != null ? _i : void 0,
@@ -3378,6 +3388,9 @@ function isFileId(data, prefixes) {
   if (!prefixes) return false;
   return prefixes.some((prefix) => data.startsWith(prefix));
 }
+function toResponsesItemId(id, prefix) {
+  return typeof id === "string" && id.startsWith(prefix) ? id : void 0;
+}
 async function convertToOpenAIResponsesInput({
   prompt,
   toolNameMapping,
@@ -3531,7 +3544,7 @@ async function convertToOpenAIResponsesInput({
           switch (part.type) {
             case "text": {
               const providerOpts = (_a2 = part.providerOptions) == null ? void 0 : _a2[providerOptionsName];
-              const id = providerOpts == null ? void 0 : providerOpts.itemId;
+              const id = toResponsesItemId(providerOpts == null ? void 0 : providerOpts.itemId, "msg");
               const phase = providerOpts == null ? void 0 : providerOpts.phase;
               if (hasConversation && id != null) {
                 break;
@@ -3541,9 +3554,11 @@ async function convertToOpenAIResponsesInput({
                 break;
               }
               input.push({
+                type: "message",
                 role: "assistant",
                 content: [{ type: "output_text", text: part.text }],
                 id,
+                status: "completed",
                 ...phase != null && { phase }
               });
               break;
@@ -3764,7 +3779,7 @@ async function convertToOpenAIResponsesInput({
                 providerOptions: part.providerOptions,
                 schema: openaiResponsesReasoningProviderOptionsSchema
               });
-              const reasoningId = providerOptions2 == null ? void 0 : providerOptions2.itemId;
+              const reasoningId = toResponsesItemId(providerOptions2 == null ? void 0 : providerOptions2.itemId, "rs");
               if ((hasConversation || hasPreviousResponseId) && reasoningId != null) {
                 break;
               }
@@ -3779,6 +3794,12 @@ async function convertToOpenAIResponsesInput({
                       summary: []
                     };
                   }
+                } else if ((providerOptions2 == null ? void 0 : providerOptions2.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   const summaryParts = [];
                   if (part.text.length > 0) {
@@ -3822,6 +3843,12 @@ async function convertToOpenAIResponsesInput({
                     encrypted_content: encryptedContent,
                     summary: summaryParts
                   });
+                } else if ((providerOptions2 == null ? void 0 : providerOptions2.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   warnings.push({
                     type: "other",
@@ -4104,21 +4131,22 @@ ${text}`,
     }
   }
   if (!store && input.some(
-    (item) => "type" in item && item.type === "reasoning" && item.encrypted_content == null
+    (item) => "type" in item && item.type === "reasoning" && item.encrypted_content == null && !("content" in item && item.content != null)
   )) {
     warnings.push({
       type: "other",
       message: "Reasoning parts without encrypted content are not supported when store is false. Skipping reasoning parts."
     });
     input = input.filter(
-      (item) => !("type" in item) || item.type !== "reasoning" || item.encrypted_content != null
+      (item) => !("type" in item) || item.type !== "reasoning" || item.encrypted_content != null || "content" in item && item.content != null
     );
   }
   return { input, warnings };
 }
 var openaiResponsesReasoningProviderOptionsSchema = z21.object({
   itemId: z21.string().nullish(),
-  reasoningEncryptedContent: z21.string().nullish()
+  reasoningEncryptedContent: z21.string().nullish(),
+  rawReasoningContent: z21.boolean().nullish()
 });
 
 // src/responses/map-openai-responses-finish-reason.ts
@@ -4759,6 +4787,11 @@ var openaiResponsesChunkSchema = lazySchema20(
         summary_index: z22.number(),
         delta: z22.string()
       }),
+      z22.object({
+        type: z22.literal("response.reasoning_text.delta"),
+        item_id: z22.string(),
+        delta: z22.string()
+      }),
       z22.object({
         type: z22.literal("response.reasoning_summary_part.done"),
         item_id: z22.string(),
@@ -4853,7 +4886,7 @@ var openaiResponsesResponseSchema = lazySchema20(
                       index: z22.number()
                     })
                   ])
-                )
+                ).nullish().transform((v) => v ?? [])
               })
             )
           }),
@@ -6066,7 +6099,7 @@ var OpenAIResponsesLanguageModel = class {
         details: "promptCacheRetention is not supported by GPT-6 and later models; use promptCacheOptions instead"
       });
     }
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (!((openaiOptions == null ? void 0 : openaiOptions.reasoningEffort) === "none" && modelCapabilities.supportsNonReasoningParameters)) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -6085,7 +6118,7 @@ var OpenAIResponsesLanguageModel = class {
           });
         }
       }
-      if (modelCapabilities.supportedReasoningEfforts != null && (baseArgs.top_logprobs != null || ((_i = baseArgs.include) == null ? void 0 : _i.includes("message.output_text.logprobs")))) {
+      if (modelCapabilities.supportedReasoningEfforts != null && !(resolvedReasoningEffort === "none" && modelCapabilities.supportsNonReasoningParameters) && (baseArgs.top_logprobs != null || ((_i = baseArgs.include) == null ? void 0 : _i.includes("message.output_text.logprobs")))) {
         baseArgs.top_logprobs = void 0;
         const filteredInclude = (_j = baseArgs.include) == null ? void 0 : _j.filter(
           (value) => value !== "message.output_text.logprobs"
@@ -7517,6 +7550,17 @@ var OpenAIResponsesLanguageModel = class {
                   }
                 }
               });
+            } else if (value.type === "response.reasoning_text.delta") {
+              controller.enqueue({
+                type: "reasoning-delta",
+                id: `${value.item_id}:0`,
+                delta: value.delta,
+                providerMetadata: {
+                  [providerOptionsName]: {
+                    itemId: value.item_id
+                  }
+                }
+              });
             } else if (value.type === "response.reasoning_summary_part.done") {
               if (store) {
                 controller.enqueue({
diff --git a/dist/internal/index.js b/dist/internal/index.js
index e5b1f96..9f18b7e 100644
--- a/dist/internal/index.js
+++ b/dist/internal/index.js
@@ -95,16 +95,17 @@ function getOpenAILanguageModelCapabilities(modelId) {
   const isGptChatModel = (gptVersion == null ? void 0 : gptVersion.minor) == null && ((_b = (_a2 = gptVersion == null ? void 0 : gptVersion.variant) == null ? void 0 : _a2.startsWith("chat")) != null ? _b : false);
   const isGptNanoModel = (_d = (_c = gptVersion == null ? void 0 : gptVersion.variant) == null ? void 0 : _c.startsWith("nano")) != null ? _d : false;
   const isGpt6OrLaterModel = gptVersion != null && gptVersion.major >= 6;
+  const isGpt6NonReasoningModel = /^gpt-6-(?:sol|luna)(?:-|$)/.test(modelId);
   const supportsFlexProcessing = oSeriesVersion != null && oSeriesVersion >= 3 || gptVersion != null && gptVersion.major >= 5 && !isGptChatModel;
   const supportsPriorityProcessing = modelId.startsWith("gpt-4") || gptVersion != null && gptVersion.major >= 5 && !isGptNanoModel && !isGptChatModel || oSeriesVersion != null && oSeriesVersion >= 3;
   const isReasoningModel = oSeriesVersion != null || gptVersion != null && gptVersion.major >= 5 && !isGptChatModel;
-  const supportsNonReasoningParameters = !isGpt6OrLaterModel && gptVersion != null && (gptVersion.major > 5 || gptVersion.major === 5 && ((_e = gptVersion.minor) != null ? _e : 0) >= 1);
+  const supportsNonReasoningParameters = isGpt6NonReasoningModel || (!isGpt6OrLaterModel && gptVersion != null && (gptVersion.major > 5 || gptVersion.major === 5 && ((_e = gptVersion.minor) != null ? _e : 0) >= 1));
   const systemMessageMode = isReasoningModel ? "developer" : "system";
   return {
     supportsFlexProcessing,
     supportsPriorityProcessing,
     supportsConfigurationUpdate: isGpt6OrLaterModel,
-    supportedReasoningEfforts: isGpt6OrLaterModel ? ["low", "medium", "high", "xhigh", "max"] : void 0,
+    supportedReasoningEfforts: isGpt6OrLaterModel ? isGpt6NonReasoningModel ? ["none", "low", "medium", "high", "xhigh", "max"] : ["low", "medium", "high", "xhigh", "max"] : void 0,
     isReasoningModel,
     systemMessageMode,
     supportsNonReasoningParameters
@@ -1047,7 +1048,7 @@ var OpenAIChatLanguageModel = class {
         details: "promptCacheRetention is not supported by GPT-6 and later models; use promptCacheOptions instead"
       });
     }
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (openaiOptions.reasoningEffort !== "none" || !modelCapabilities.supportsNonReasoningParameters) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -2060,7 +2061,8 @@ var openaiImageResponseSchema = (0, import_provider_utils12.lazySchema)(
       created: import_v48.z.number().nullish(),
       data: import_v48.z.array(
         import_v48.z.object({
-          b64_json: import_v48.z.string(),
+          b64_json: import_v48.z.string().nullish(),
+          url: import_v48.z.string().nullish(),
           revised_prompt: import_v48.z.string().nullish()
         })
       ),
@@ -2248,7 +2250,11 @@ var OpenAIImageModel = class {
         fetch: this.config.fetch
       });
       return {
-        images: response2.data.map((item) => item.b64_json),
+        images: response2.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
         warnings,
         usage: response2.usage != null ? {
           inputTokens: (_e = response2.usage.input_tokens) != null ? _e : void 0,
@@ -2315,7 +2321,11 @@ var OpenAIImageModel = class {
       fetch: this.config.fetch
     });
     return {
-      images: response.data.map((item) => item.b64_json),
+      images: response.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
       warnings,
       usage: response.usage != null ? {
         inputTokens: (_i = response.usage.input_tokens) != null ? _i : void 0,
@@ -3224,6 +3234,9 @@ function isFileId(data, prefixes) {
   if (!prefixes) return false;
   return prefixes.some((prefix) => data.startsWith(prefix));
 }
+function toResponsesItemId(id, prefix) {
+  return typeof id === "string" && id.startsWith(prefix) ? id : void 0;
+}
 async function convertToOpenAIResponsesInput({
   prompt,
   toolNameMapping,
@@ -3377,7 +3390,7 @@ async function convertToOpenAIResponsesInput({
           switch (part.type) {
             case "text": {
               const providerOpts = (_a2 = part.providerOptions) == null ? void 0 : _a2[providerOptionsName];
-              const id = providerOpts == null ? void 0 : providerOpts.itemId;
+              const id = toResponsesItemId(providerOpts == null ? void 0 : providerOpts.itemId, "msg");
               const phase = providerOpts == null ? void 0 : providerOpts.phase;
               if (hasConversation && id != null) {
                 break;
@@ -3387,9 +3400,11 @@ async function convertToOpenAIResponsesInput({
                 break;
               }
               input.push({
+                type: "message",
                 role: "assistant",
                 content: [{ type: "output_text", text: part.text }],
                 id,
+                status: "completed",
                 ...phase != null && { phase }
               });
               break;
@@ -3610,7 +3625,7 @@ async function convertToOpenAIResponsesInput({
                 providerOptions: part.providerOptions,
                 schema: openaiResponsesReasoningProviderOptionsSchema
               });
-              const reasoningId = providerOptions2 == null ? void 0 : providerOptions2.itemId;
+              const reasoningId = toResponsesItemId(providerOptions2 == null ? void 0 : providerOptions2.itemId, "rs");
               if ((hasConversation || hasPreviousResponseId) && reasoningId != null) {
                 break;
               }
@@ -3625,6 +3640,12 @@ async function convertToOpenAIResponsesInput({
                       summary: []
                     };
                   }
+                } else if ((providerOptions2 == null ? void 0 : providerOptions2.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   const summaryParts = [];
                   if (part.text.length > 0) {
@@ -3668,6 +3689,12 @@ async function convertToOpenAIResponsesInput({
                     encrypted_content: encryptedContent,
                     summary: summaryParts
                   });
+                } else if ((providerOptions2 == null ? void 0 : providerOptions2.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   warnings.push({
                     type: "other",
@@ -3950,21 +3977,22 @@ ${text}`,
     }
   }
   if (!store && input.some(
-    (item) => "type" in item && item.type === "reasoning" && item.encrypted_content == null
+    (item) => "type" in item && item.type === "reasoning" && item.encrypted_content == null && !("content" in item && item.content != null)
   )) {
     warnings.push({
       type: "other",
       message: "Reasoning parts without encrypted content are not supported when store is false. Skipping reasoning parts."
     });
     input = input.filter(
-      (item) => !("type" in item) || item.type !== "reasoning" || item.encrypted_content != null
+      (item) => !("type" in item) || item.type !== "reasoning" || item.encrypted_content != null || "content" in item && item.content != null
     );
   }
   return { input, warnings };
 }
 var openaiResponsesReasoningProviderOptionsSchema = import_v417.z.object({
   itemId: import_v417.z.string().nullish(),
-  reasoningEncryptedContent: import_v417.z.string().nullish()
+  reasoningEncryptedContent: import_v417.z.string().nullish(),
+  rawReasoningContent: import_v417.z.boolean().nullish()
 });
 
 // src/responses/map-openai-responses-finish-reason.ts
@@ -4602,6 +4630,11 @@ var openaiResponsesChunkSchema = (0, import_provider_utils26.lazySchema)(
         summary_index: import_v418.z.number(),
         delta: import_v418.z.string()
       }),
+      import_v418.z.object({
+        type: import_v418.z.literal("response.reasoning_text.delta"),
+        item_id: import_v418.z.string(),
+        delta: import_v418.z.string()
+      }),
       import_v418.z.object({
         type: import_v418.z.literal("response.reasoning_summary_part.done"),
         item_id: import_v418.z.string(),
@@ -4696,7 +4729,7 @@ var openaiResponsesResponseSchema = (0, import_provider_utils26.lazySchema)(
                       index: import_v418.z.number()
                     })
                   ])
-                )
+                ).nullish().transform((v) => v ?? [])
               })
             )
           }),
@@ -6228,7 +6261,7 @@ var OpenAIResponsesLanguageModel = class {
         details: "promptCacheRetention is not supported by GPT-6 and later models; use promptCacheOptions instead"
       });
     }
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (!((openaiOptions == null ? void 0 : openaiOptions.reasoningEffort) === "none" && modelCapabilities.supportsNonReasoningParameters)) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -6247,7 +6280,7 @@ var OpenAIResponsesLanguageModel = class {
           });
         }
       }
-      if (modelCapabilities.supportedReasoningEfforts != null && (baseArgs.top_logprobs != null || ((_i = baseArgs.include) == null ? void 0 : _i.includes("message.output_text.logprobs")))) {
+      if (modelCapabilities.supportedReasoningEfforts != null && !(resolvedReasoningEffort === "none" && modelCapabilities.supportsNonReasoningParameters) && (baseArgs.top_logprobs != null || ((_i = baseArgs.include) == null ? void 0 : _i.includes("message.output_text.logprobs")))) {
         baseArgs.top_logprobs = void 0;
         const filteredInclude = (_j = baseArgs.include) == null ? void 0 : _j.filter(
           (value) => value !== "message.output_text.logprobs"
@@ -7679,6 +7712,17 @@ var OpenAIResponsesLanguageModel = class {
                   }
                 }
               });
+            } else if (value.type === "response.reasoning_text.delta") {
+              controller.enqueue({
+                type: "reasoning-delta",
+                id: `${value.item_id}:0`,
+                delta: value.delta,
+                providerMetadata: {
+                  [providerOptionsName]: {
+                    itemId: value.item_id
+                  }
+                }
+              });
             } else if (value.type === "response.reasoning_summary_part.done") {
               if (store) {
                 controller.enqueue({
diff --git a/dist/internal/index.mjs b/dist/internal/index.mjs
index 714e237..a5417b6 100644
--- a/dist/internal/index.mjs
+++ b/dist/internal/index.mjs
@@ -38,16 +38,17 @@ function getOpenAILanguageModelCapabilities(modelId) {
   const isGptChatModel = (gptVersion == null ? void 0 : gptVersion.minor) == null && ((_b = (_a2 = gptVersion == null ? void 0 : gptVersion.variant) == null ? void 0 : _a2.startsWith("chat")) != null ? _b : false);
   const isGptNanoModel = (_d = (_c = gptVersion == null ? void 0 : gptVersion.variant) == null ? void 0 : _c.startsWith("nano")) != null ? _d : false;
   const isGpt6OrLaterModel = gptVersion != null && gptVersion.major >= 6;
+  const isGpt6NonReasoningModel = /^gpt-6-(?:sol|luna)(?:-|$)/.test(modelId);
   const supportsFlexProcessing = oSeriesVersion != null && oSeriesVersion >= 3 || gptVersion != null && gptVersion.major >= 5 && !isGptChatModel;
   const supportsPriorityProcessing = modelId.startsWith("gpt-4") || gptVersion != null && gptVersion.major >= 5 && !isGptNanoModel && !isGptChatModel || oSeriesVersion != null && oSeriesVersion >= 3;
   const isReasoningModel = oSeriesVersion != null || gptVersion != null && gptVersion.major >= 5 && !isGptChatModel;
-  const supportsNonReasoningParameters = !isGpt6OrLaterModel && gptVersion != null && (gptVersion.major > 5 || gptVersion.major === 5 && ((_e = gptVersion.minor) != null ? _e : 0) >= 1);
+  const supportsNonReasoningParameters = isGpt6NonReasoningModel || (!isGpt6OrLaterModel && gptVersion != null && (gptVersion.major > 5 || gptVersion.major === 5 && ((_e = gptVersion.minor) != null ? _e : 0) >= 1));
   const systemMessageMode = isReasoningModel ? "developer" : "system";
   return {
     supportsFlexProcessing,
     supportsPriorityProcessing,
     supportsConfigurationUpdate: isGpt6OrLaterModel,
-    supportedReasoningEfforts: isGpt6OrLaterModel ? ["low", "medium", "high", "xhigh", "max"] : void 0,
+    supportedReasoningEfforts: isGpt6OrLaterModel ? isGpt6NonReasoningModel ? ["none", "low", "medium", "high", "xhigh", "max"] : ["low", "medium", "high", "xhigh", "max"] : void 0,
     isReasoningModel,
     systemMessageMode,
     supportsNonReasoningParameters
@@ -1000,7 +1001,7 @@ var OpenAIChatLanguageModel = class {
         details: "promptCacheRetention is not supported by GPT-6 and later models; use promptCacheOptions instead"
       });
     }
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (openaiOptions.reasoningEffort !== "none" || !modelCapabilities.supportsNonReasoningParameters) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -2048,7 +2049,8 @@ var openaiImageResponseSchema = lazySchema7(
       created: z8.number().nullish(),
       data: z8.array(
         z8.object({
-          b64_json: z8.string(),
+          b64_json: z8.string().nullish(),
+          url: z8.string().nullish(),
           revised_prompt: z8.string().nullish()
         })
       ),
@@ -2239,7 +2241,11 @@ var OpenAIImageModel = class {
         fetch: this.config.fetch
       });
       return {
-        images: response2.data.map((item) => item.b64_json),
+        images: response2.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
         warnings,
         usage: response2.usage != null ? {
           inputTokens: (_e = response2.usage.input_tokens) != null ? _e : void 0,
@@ -2306,7 +2312,11 @@ var OpenAIImageModel = class {
       fetch: this.config.fetch
     });
     return {
-      images: response.data.map((item) => item.b64_json),
+      images: response.data.flatMap((item) => {
+          if (typeof item.b64_json === "string") return [item.b64_json];
+          if (typeof item.url === "string") return [item.url];
+          return [];
+        }),
       warnings,
       usage: response.usage != null ? {
         inputTokens: (_i = response.usage.input_tokens) != null ? _i : void 0,
@@ -3269,6 +3279,9 @@ function isFileId(data, prefixes) {
   if (!prefixes) return false;
   return prefixes.some((prefix) => data.startsWith(prefix));
 }
+function toResponsesItemId(id, prefix) {
+  return typeof id === "string" && id.startsWith(prefix) ? id : void 0;
+}
 async function convertToOpenAIResponsesInput({
   prompt,
   toolNameMapping,
@@ -3422,7 +3435,7 @@ async function convertToOpenAIResponsesInput({
           switch (part.type) {
             case "text": {
               const providerOpts = (_a2 = part.providerOptions) == null ? void 0 : _a2[providerOptionsName];
-              const id = providerOpts == null ? void 0 : providerOpts.itemId;
+              const id = toResponsesItemId(providerOpts == null ? void 0 : providerOpts.itemId, "msg");
               const phase = providerOpts == null ? void 0 : providerOpts.phase;
               if (hasConversation && id != null) {
                 break;
@@ -3432,9 +3445,11 @@ async function convertToOpenAIResponsesInput({
                 break;
               }
               input.push({
+                type: "message",
                 role: "assistant",
                 content: [{ type: "output_text", text: part.text }],
                 id,
+                status: "completed",
                 ...phase != null && { phase }
               });
               break;
@@ -3655,7 +3670,7 @@ async function convertToOpenAIResponsesInput({
                 providerOptions: part.providerOptions,
                 schema: openaiResponsesReasoningProviderOptionsSchema
               });
-              const reasoningId = providerOptions2 == null ? void 0 : providerOptions2.itemId;
+              const reasoningId = toResponsesItemId(providerOptions2 == null ? void 0 : providerOptions2.itemId, "rs");
               if ((hasConversation || hasPreviousResponseId) && reasoningId != null) {
                 break;
               }
@@ -3670,6 +3685,12 @@ async function convertToOpenAIResponsesInput({
                       summary: []
                     };
                   }
+                } else if ((providerOptions2 == null ? void 0 : providerOptions2.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   const summaryParts = [];
                   if (part.text.length > 0) {
@@ -3713,6 +3734,12 @@ async function convertToOpenAIResponsesInput({
                     encrypted_content: encryptedContent,
                     summary: summaryParts
                   });
+                } else if ((providerOptions2 == null ? void 0 : providerOptions2.rawReasoningContent) && part.text.length > 0) {
+                  input.push({
+                    type: "reasoning",
+                    summary: [],
+                    content: [{ type: "reasoning_text", text: part.text }]
+                  });
                 } else {
                   warnings.push({
                     type: "other",
@@ -3995,21 +4022,22 @@ ${text}`,
     }
   }
   if (!store && input.some(
-    (item) => "type" in item && item.type === "reasoning" && item.encrypted_content == null
+    (item) => "type" in item && item.type === "reasoning" && item.encrypted_content == null && !("content" in item && item.content != null)
   )) {
     warnings.push({
       type: "other",
       message: "Reasoning parts without encrypted content are not supported when store is false. Skipping reasoning parts."
     });
     input = input.filter(
-      (item) => !("type" in item) || item.type !== "reasoning" || item.encrypted_content != null
+      (item) => !("type" in item) || item.type !== "reasoning" || item.encrypted_content != null || "content" in item && item.content != null
     );
   }
   return { input, warnings };
 }
 var openaiResponsesReasoningProviderOptionsSchema = z17.object({
   itemId: z17.string().nullish(),
-  reasoningEncryptedContent: z17.string().nullish()
+  reasoningEncryptedContent: z17.string().nullish(),
+  rawReasoningContent: z17.boolean().nullish()
 });
 
 // src/responses/map-openai-responses-finish-reason.ts
@@ -4650,6 +4678,11 @@ var openaiResponsesChunkSchema = lazySchema16(
         summary_index: z18.number(),
         delta: z18.string()
       }),
+      z18.object({
+        type: z18.literal("response.reasoning_text.delta"),
+        item_id: z18.string(),
+        delta: z18.string()
+      }),
       z18.object({
         type: z18.literal("response.reasoning_summary_part.done"),
         item_id: z18.string(),
@@ -4744,7 +4777,7 @@ var openaiResponsesResponseSchema = lazySchema16(
                       index: z18.number()
                     })
                   ])
-                )
+                ).nullish().transform((v) => v ?? [])
               })
             )
           }),
@@ -6309,7 +6342,7 @@ var OpenAIResponsesLanguageModel = class {
         details: "promptCacheRetention is not supported by GPT-6 and later models; use promptCacheOptions instead"
       });
     }
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       if (!((openaiOptions == null ? void 0 : openaiOptions.reasoningEffort) === "none" && modelCapabilities.supportsNonReasoningParameters)) {
         if (baseArgs.temperature != null) {
           baseArgs.temperature = void 0;
@@ -6328,7 +6361,7 @@ var OpenAIResponsesLanguageModel = class {
           });
         }
       }
-      if (modelCapabilities.supportedReasoningEfforts != null && (baseArgs.top_logprobs != null || ((_i = baseArgs.include) == null ? void 0 : _i.includes("message.output_text.logprobs")))) {
+      if (modelCapabilities.supportedReasoningEfforts != null && !(resolvedReasoningEffort === "none" && modelCapabilities.supportsNonReasoningParameters) && (baseArgs.top_logprobs != null || ((_i = baseArgs.include) == null ? void 0 : _i.includes("message.output_text.logprobs")))) {
         baseArgs.top_logprobs = void 0;
         const filteredInclude = (_j = baseArgs.include) == null ? void 0 : _j.filter(
           (value) => value !== "message.output_text.logprobs"
@@ -7760,6 +7793,17 @@ var OpenAIResponsesLanguageModel = class {
                   }
                 }
               });
+            } else if (value.type === "response.reasoning_text.delta") {
+              controller.enqueue({
+                type: "reasoning-delta",
+                id: `${value.item_id}:0`,
+                delta: value.delta,
+                providerMetadata: {
+                  [providerOptionsName]: {
+                    itemId: value.item_id
+                  }
+                }
+              });
             } else if (value.type === "response.reasoning_summary_part.done") {
               if (store) {
                 controller.enqueue({
diff --git a/src/chat/openai-chat-language-model.ts b/src/chat/openai-chat-language-model.ts
index e5bda94..f31c9ff 100644
--- a/src/chat/openai-chat-language-model.ts
+++ b/src/chat/openai-chat-language-model.ts
@@ -211,7 +211,7 @@ export class OpenAIChatLanguageModel implements LanguageModelV3 {
 
     // remove unsupported settings for reasoning models
     // see https://platform.openai.com/docs/guides/reasoning#limitations
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       // when reasoning effort is none, gpt-5.1 models allow temperature, topP, logprobs
       //  https://platform.openai.com/docs/guides/latest-model#gpt-5-1-parameter-compatibility
       if (
diff --git a/src/image/openai-image-api.ts b/src/image/openai-image-api.ts
index de779ca..14b7247 100644
--- a/src/image/openai-image-api.ts
+++ b/src/image/openai-image-api.ts
@@ -9,7 +9,8 @@ export const openaiImageResponseSchema = lazySchema(() =>
       created: z.number().nullish(),
       data: z.array(
         z.object({
-          b64_json: z.string(),
+          b64_json: z.string().nullish(),
+          url: z.string().nullish(),
           revised_prompt: z.string().nullish(),
         }),
       ),
diff --git a/src/image/openai-image-model.ts b/src/image/openai-image-model.ts
index 41b81b2..f0858ff 100644
--- a/src/image/openai-image-model.ts
+++ b/src/image/openai-image-model.ts
@@ -132,7 +132,11 @@ export class OpenAIImageModel implements ImageModelV3 {
       });
 
       return {
-        images: response.data.map(item => item.b64_json),
+        images: response.data.flatMap(item => {
+        if (typeof item.b64_json === 'string') return [item.b64_json];
+        if (typeof item.url === 'string') return [item.url];
+        return [];
+      }),
         warnings,
         usage:
           response.usage != null
@@ -207,7 +211,11 @@ export class OpenAIImageModel implements ImageModelV3 {
     });
 
     return {
-      images: response.data.map(item => item.b64_json),
+      images: response.data.flatMap(item => {
+        if (typeof item.b64_json === 'string') return [item.b64_json];
+        if (typeof item.url === 'string') return [item.url];
+        return [];
+      }),
       warnings,
       usage:
         response.usage != null
diff --git a/src/openai-language-model-capabilities.ts b/src/openai-language-model-capabilities.ts
index c4b8d3a..d02adb4 100644
--- a/src/openai-language-model-capabilities.ts
+++ b/src/openai-language-model-capabilities.ts
@@ -22,6 +22,7 @@ export function getOpenAILanguageModelCapabilities(
     (gptVersion?.variant?.startsWith('chat') ?? false);
   const isGptNanoModel = gptVersion?.variant?.startsWith('nano') ?? false;
   const isGpt6OrLaterModel = gptVersion != null && gptVersion.major >= 6;
+  const isGpt6NonReasoningModel = /^gpt-6-(?:sol|luna)(?:-|$)/.test(modelId);
 
   const supportsFlexProcessing =
     (oSeriesVersion != null && oSeriesVersion >= 3) ||
@@ -44,10 +45,11 @@ export function getOpenAILanguageModelCapabilities(
   // https://platform.openai.com/docs/guides/latest-model#gpt-5-1-parameter-compatibility
   // GPT-5.1 and later model families support temperature, topP, logProbs when reasoningEffort is none.
   const supportsNonReasoningParameters =
-    !isGpt6OrLaterModel &&
-    gptVersion != null &&
-    (gptVersion.major > 5 ||
-      (gptVersion.major === 5 && (gptVersion.minor ?? 0) >= 1));
+    isGpt6NonReasoningModel ||
+    (!isGpt6OrLaterModel &&
+      gptVersion != null &&
+      (gptVersion.major > 5 ||
+        (gptVersion.major === 5 && (gptVersion.minor ?? 0) >= 1)));
 
   const systemMessageMode = isReasoningModel ? 'developer' : 'system';
 
@@ -56,7 +58,9 @@ export function getOpenAILanguageModelCapabilities(
     supportsPriorityProcessing,
     supportsConfigurationUpdate: isGpt6OrLaterModel,
     supportedReasoningEfforts: isGpt6OrLaterModel
-      ? ['low', 'medium', 'high', 'xhigh', 'max']
+      ? isGpt6NonReasoningModel
+        ? ['none', 'low', 'medium', 'high', 'xhigh', 'max']
+        : ['low', 'medium', 'high', 'xhigh', 'max']
       : undefined,
     isReasoningModel,
     systemMessageMode,
diff --git a/src/responses/convert-to-openai-responses-input.ts b/src/responses/convert-to-openai-responses-input.ts
index 5c27d8f..703956b 100644
--- a/src/responses/convert-to-openai-responses-input.ts
+++ b/src/responses/convert-to-openai-responses-input.ts
@@ -291,6 +291,14 @@ function isFileId(data: string, prefixes?: readonly string[]): boolean {
   return prefixes.some(prefix => data.startsWith(prefix));
 }
 
+/** Responses item ids are prefix-tagged (`msg_…`, `rs_…`); UUIDs 400. */
+function toResponsesItemId(
+  id: unknown,
+  prefix: 'msg' | 'rs',
+): string | undefined {
+  return typeof id === 'string' && id.startsWith(prefix) ? id : undefined;
+}
+
 export async function convertToOpenAIResponsesInput({
   prompt,
   toolNameMapping,
@@ -495,7 +503,7 @@ export async function convertToOpenAIResponsesInput({
           switch (part.type) {
             case 'text': {
               const providerOpts = part.providerOptions?.[providerOptionsName];
-              const id = providerOpts?.itemId as string | undefined;
+              const id = toResponsesItemId(providerOpts?.itemId, 'msg');
               const phase = providerOpts?.phase as
                 | 'commentary'
                 | 'final_answer'
@@ -514,9 +522,14 @@ export async function convertToOpenAIResponsesInput({
               }
 
               input.push({
+                // Ark rejects assistant input items that omit either field (#18253);
+                // both are the canonical Responses shape OpenAI accepts. Only the
+                // assistant item needs it — Ark takes user/system items as-is.
+                type: 'message',
                 role: 'assistant',
                 content: [{ type: 'output_text', text: part.text }],
                 id,
+                status: 'completed',
                 ...(phase != null && { phase }),
               });
 
@@ -868,7 +881,7 @@ export async function convertToOpenAIResponsesInput({
                 schema: openaiResponsesReasoningProviderOptionsSchema,
               });
 
-              const reasoningId = providerOptions?.itemId;
+              const reasoningId = toResponsesItemId(providerOptions?.itemId, 'rs');
 
               if (
                 (hasConversation || hasPreviousResponseId) &&
@@ -893,6 +906,15 @@ export async function convertToOpenAIResponsesInput({
                       summary: [],
                     };
                   }
+                } else if (
+                  providerOptions?.rawReasoningContent &&
+                  part.text.length > 0
+                ) {
+                  input.push({
+                    type: 'reasoning',
+                    summary: [],
+                    content: [{ type: 'reasoning_text', text: part.text }],
+                  });
                 } else {
                   const summaryParts: Array<{
                     type: 'summary_text';
@@ -955,6 +977,17 @@ export async function convertToOpenAIResponsesInput({
                     encrypted_content: encryptedContent,
                     summary: summaryParts,
                   });
+                } else if (
+                  providerOptions?.rawReasoningContent &&
+                  part.text.length > 0
+                ) {
+                  // Thinking dialects (DeepSeek, …) require the reasoning text itself on
+                  // replay; opt-in because OpenAI rejects `content` on reasoning items.
+                  input.push({
+                    type: 'reasoning',
+                    summary: [],
+                    content: [{ type: 'reasoning_text', text: part.text }],
+                  });
                 } else {
                   warnings.push({
                     type: 'other',
@@ -1313,7 +1346,8 @@ export async function convertToOpenAIResponsesInput({
       item =>
         'type' in item &&
         item.type === 'reasoning' &&
-        item.encrypted_content == null,
+        item.encrypted_content == null &&
+        !('content' in item && item.content != null),
     )
   ) {
     warnings.push({
@@ -1325,7 +1359,8 @@ export async function convertToOpenAIResponsesInput({
       item =>
         !('type' in item) ||
         item.type !== 'reasoning' ||
-        item.encrypted_content != null,
+        item.encrypted_content != null ||
+        ('content' in item && item.content != null),
     );
   }
 
@@ -1335,6 +1370,7 @@ export async function convertToOpenAIResponsesInput({
 const openaiResponsesReasoningProviderOptionsSchema = z.object({
   itemId: z.string().nullish(),
   reasoningEncryptedContent: z.string().nullish(),
+  rawReasoningContent: z.boolean().nullish(),
 });
 
 export type OpenAIResponsesReasoningProviderOptions = z.infer<
diff --git a/src/responses/openai-responses-api.ts b/src/responses/openai-responses-api.ts
index aef872b..b34fcfd 100644
--- a/src/responses/openai-responses-api.ts
+++ b/src/responses/openai-responses-api.ts
@@ -1206,6 +1206,11 @@ export const openaiResponsesChunkSchema = lazySchema(() =>
         summary_index: z.number(),
         delta: z.string(),
       }),
+      z.object({
+        type: z.literal('response.reasoning_text.delta'),
+        item_id: z.string(),
+        delta: z.string(),
+      }),
       z.object({
         type: z.literal('response.reasoning_summary_part.done'),
         item_id: z.string(),
@@ -1327,7 +1332,7 @@ export const openaiResponsesResponseSchema = lazySchema(() =>
                         index: z.number(),
                       }),
                     ]),
-                  ),
+                  ).nullish().transform((v) => v ?? []),
                 }),
               ),
             }),
diff --git a/src/responses/openai-responses-language-model.ts b/src/responses/openai-responses-language-model.ts
index 485880b..a53bbf8 100644
--- a/src/responses/openai-responses-language-model.ts
+++ b/src/responses/openai-responses-language-model.ts
@@ -436,7 +436,7 @@ export class OpenAIResponsesLanguageModel implements LanguageModelV3 {
 
     // remove unsupported settings for reasoning models
     // see https://platform.openai.com/docs/guides/reasoning#limitations
-    if (isReasoningModel) {
+    if (modelCapabilities.isReasoningModel) {
       // when reasoning effort is none, gpt-5.1 models allow temperature, topP, logprobs
       //  https://platform.openai.com/docs/guides/latest-model#gpt-5-1-parameter-compatibility
       if (
@@ -466,6 +466,7 @@ export class OpenAIResponsesLanguageModel implements LanguageModelV3 {
 
       if (
         modelCapabilities.supportedReasoningEfforts != null &&
+        !(resolvedReasoningEffort === 'none' && modelCapabilities.supportsNonReasoningParameters) &&
         (baseArgs.top_logprobs != null ||
           baseArgs.include?.includes('message.output_text.logprobs'))
       ) {
@@ -2243,6 +2244,17 @@ export class OpenAIResponsesLanguageModel implements LanguageModelV3 {
                   } satisfies ResponsesReasoningProviderMetadata,
                 },
               });
+            } else if (value.type === 'response.reasoning_text.delta') {
+              controller.enqueue({
+                type: 'reasoning-delta',
+                id: `${value.item_id}:0`,
+                delta: value.delta,
+                providerMetadata: {
+                  [providerOptionsName]: {
+                    itemId: value.item_id,
+                  } satisfies ResponsesReasoningProviderMetadata,
+                },
+              });
             } else if (value.type === 'response.reasoning_summary_part.done') {
               // when OpenAI stores the message data, we can immediately conclude the reasoning part
               // since we do not need to send the encrypted content.
